import { Button } from "@chakra-ui/react";

export default function DisableAccountButton() {
    return (
        <>
            <Button variant="outline" colorPalette="red">
                Disable Account
            </Button>
        </>
    );
}